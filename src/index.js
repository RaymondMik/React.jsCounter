import React from 'react';
import ReactDOM from 'react-dom';
import ReactCounter from './App';
import './index.css';

ReactDOM.render(
  <ReactCounter />,
  document.getElementById('app-container')
);
